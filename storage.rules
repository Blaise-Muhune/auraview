rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Feed images: authenticated users can write to their own folder, anyone can read (for public feed)
    match /feed/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
